#include<iostream>

using namespace std;

#define MAX 100001

int dp[MAX] = { 0, };

int main() {
	ios_base::sync_with_stdio(0); cin.tie(0);
	int Num, count = 0;
	cin >> Num;


	cout << count << "\n";

	return 0;
}